name: PR Debug - Weekly Update Test (Actually Update README)
on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main, master]
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR 번호 (선택사항)'
        required: false
        type: string
      force_reset:
        description: '강제 리셋'
        required: false
        default: true
        type: boolean
      notification:
        description: '알림 전송 여부'
        required: false
        default: false
        type: boolean
      actually_commit:
        description: '실제로 README 커밋하기'
        required: false
        default: true
        type: boolean

permissions:
  contents: write  # README 커밋을 위해 write 권한 필요
  pull-requests: write
  issues: write
  actions: read

env:
  TZ: Asia/Seoul
  DEBUG_MODE: true
  FORCE_RESET: true

jobs:
  pr-debug-with-commit:
    runs-on: ubuntu-latest
    steps:
      - name: PR Debug Info
        run: |
          echo "🔥 PR DEBUG: README 실제 업데이트 (커밋 포함)"
          echo "⏰ 실행 시간: $(date '+%Y-%m-%d %H:%M:%S KST')"
          echo "🔧 트리거: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "📝 PR 번호: #${{ github.event.number }}"
            echo "👤 PR 작성자: ${{ github.event.pull_request.user.login }}"
            echo "🌿 브랜치: ${{ github.event.pull_request.head.ref }}"
            echo "📄 제목: ${{ github.event.pull_request.title }}"
          fi
          echo "👤 실행자: ${{ github.actor }}"
          echo "💾 실제 커밋: ${{ github.event.inputs.actually_commit || 'true' }}"
          
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          # main 브랜치로 체크아웃하여 실제 README 업데이트
          ref: main
          
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
          
      - name: Install dependencies
        run: |
          pip install requests pytz
          echo "📦 의존성 설치 완료"
          
      - name: Debug - Check files structure
        run: |
          echo "📁 파일 구조 확인 (main 브랜치):"
          echo "=== Root Directory ==="
          ls -la
          echo ""
          echo "=== Scripts Directory ==="
          if [ -d "scripts" ]; then
            ls -la scripts/
          else
            echo "❌ scripts 디렉토리 없음"
          fi
          echo ""
          echo "=== 사용자 디렉토리 확인 ==="
          for dir in */; do
            if [[ "$dir" != ".git/" && "$dir" != ".github/" && "$dir" != "scripts/" && "$dir" != "docs/" ]]; then
              echo "📂 $dir"
              if [ -d "$dir" ]; then
                find "$dir" -name "*.java" | head -5
              fi
            fi
          done
          
      - name: Check current session info using script
        id: session_check
        run: |
          echo "🔍 회차 정보 확인 중 (실제 업데이트 모드)..."
          
          if [ -f "scripts/check_session.py" ]; then
            echo "📝 scripts/check_session.py 실행 중..."
            
            cd scripts
            RESULT=$(python check_session.py 2>&1)
            cd ..
            
            echo "📋 스크립트 실행 결과:"
            echo "$RESULT"
            
            echo "$RESULT" | grep "=" >> $GITHUB_ENV
            
            echo "✅ check_session.py 실행 완료"
          else
            echo "❌ scripts/check_session.py 없음 - 실제 회차 정보 생성"
            
            TODAY=$(date '+%Y-%m-%d')
            WEEK_END=$(date -d '+6 days' '+%Y-%m-%d')
            
            # 실제 회차 번호 사용 (PR 번호 기반이 아닌)
            SESSION_NUM="2"  # 실제 현재 회차
            
            echo "IS_NEW_WEEK=true" >> $GITHUB_ENV
            echo "SESSION_NUMBER=$SESSION_NUM" >> $GITHUB_ENV
            echo "TODAY=$TODAY" >> $GITHUB_ENV
            echo "WEEK_START=$TODAY" >> $GITHUB_ENV
            echo "WEEK_END=$WEEK_END" >> $GITHUB_ENV
            echo "DEADLINE=$WEEK_END 23:59" >> $GITHUB_ENV
            echo "HAS_SESSION_COUNTER=false" >> $GITHUB_ENV
            echo "TOTAL_WEEKS=1" >> $GITHUB_ENV
            echo "TOTAL_DAYS=7" >> $GITHUB_ENV
            echo "STUDY_START=2025-07-21" >> $GITHUB_ENV
          fi
          
      - name: Debug - Show environment variables
        run: |
          echo "🐛 환경 변수 확인 (실제 업데이트):"
          echo "  IS_NEW_WEEK: $IS_NEW_WEEK"
          echo "  FORCE_RESET: $FORCE_RESET"
          echo "  SESSION_NUMBER: $SESSION_NUMBER"
          echo "  TODAY: $TODAY"
          echo "  WEEK_START: $WEEK_START"
          echo "  WEEK_END: $WEEK_END"
          echo "  DEADLINE: $DEADLINE"
          echo "  HAS_SESSION_COUNTER: $HAS_SESSION_COUNTER"
          echo "  TOTAL_WEEKS: $TOTAL_WEEKS"
          echo "  TOTAL_DAYS: $TOTAL_DAYS"
          
      - name: Create README backup
        run: |
          if [ -f "README.md" ]; then
            BACKUP_NAME="README_backup_$(date +%Y%m%d_%H%M%S)_pr_debug.md"
            cp README.md "$BACKUP_NAME"
            echo "📁 README 백업 생성: $BACKUP_NAME"
            echo "BACKUP_FILE=$BACKUP_NAME" >> $GITHUB_ENV
          else
            echo "ℹ️ 기존 README.md 없음"
          fi
          
      - name: Generate actual README using script
        run: |
          echo "🔄 실제 README 생성 중 (디버그 트리거, 실제 업데이트)..."
          
          if [ -f "scripts/create_debug_readme.py" ]; then
            echo "📝 scripts/create_debug_readme.py 실행 중..."
            
            cd scripts
            python create_debug_readme.py --debug-mode --try-advanced
            cd ..
            
            if [ $? -eq 0 ]; then
              echo "✅ 스크립트를 통한 README 생성 성공"
              echo "UPDATE_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "❌ 스크립트를 통한 README 생성 실패"
              echo "UPDATE_SUCCESS=false" >> $GITHUB_ENV
            fi
          else
            echo "❌ scripts/create_debug_readme.py 없음 - 기본 README 생성"
            
            # 실제 현재 회차 정보로 README 생성
            echo "# 🚀 알고리즘 스터디" > README.md
            echo "" >> README.md
            echo "## 📅 ${SESSION_NUMBER}회차 현황" >> README.md
            echo "**기간**: ${WEEK_START} ~ ${WEEK_END}" >> README.md
            echo "**마감**: ${DEADLINE}" >> README.md
            echo "" >> README.md
            echo "### 제출 현황" >> README.md
            echo "" >> README.md
            echo "| 참가자 | 월 | 화 | 수 | 목 | 금 | 토 | 일 |" >> README.md
            echo "|--------|----|----|----|----|----|----|---|" >> README.md
            echo "|        | 07/21 | 07/22 | 07/23 | 07/24 | 07/25 | 07/26 | 07/27 |" >> README.md
            
            # 실제 제출 현황 스캔 및 추가
            echo "📂 실제 제출 현황 스캔 중..."
            for user_dir in */; do
              if [[ "$user_dir" != ".git/" && "$user_dir" != ".github/" && "$user_dir" != "scripts/" && "$user_dir" != "docs/" ]]; then
                user_name=$(basename "$user_dir")
                problems=()
                
                if [ -d "$user_dir" ]; then
                  for problem_dir in "$user_dir"*/; do
                    if [ -d "$problem_dir" ]; then
                      problem_num=$(basename "$problem_dir")
                      if [[ "$problem_num" =~ ^[0-9]+$ ]]; then
                        if find "$problem_dir" -name "*.java" | grep -q .; then
                          problems+=("$problem_num")
                        fi
                      fi
                    fi
                  done
                fi
                
                if [ ${#problems[@]} -gt 0 ]; then
                  # 문제 번호 정렬
                  IFS=$'\n' sorted_problems=($(sort -n <<<"${problems[*]}"))
                  
                  # 토요일에 모든 문제 표시 (임시)
                  problem_list=""
                  if [ ${#sorted_problems[@]} -le 3 ]; then
                    problem_list=$(IFS=', '; echo "${sorted_problems[*]}")
                  else
                    problem_list="${sorted_problems[0]}, ${sorted_problems[1]}, ${sorted_problems[2]}..."
                  fi
                  
                  echo "| $user_name |  |  |  |  |  | $problem_list |  |" >> README.md
                  echo "   ✅ $user_name: ${#sorted_problems[@]}개 문제 발견"
                fi
              fi
            done
            
            echo "" >> README.md
            echo "## 🤖 자동화 시스템 소개" >> README.md
            echo "" >> README.md
            echo "### 🔧 주요 기능" >> README.md
            echo "- **자동 테스트**: 샘플 테스트케이스 + AI 생성 반례 테스트" >> README.md
            echo "- **스마트 채점**: 부분 점수 지원 (샘플만/생성 테스트만 통과)" >> README.md
            echo "- **개인 알림**: Mattermost 개인 DM으로 결과 알림" >> README.md
            echo "- **자동 README 업데이트**: 제출 현황 실시간 반영" >> README.md
            echo "- **PR 디버그**: PR 생성 시 자동 테스트 실행" >> README.md
            echo "" >> README.md
            echo "---" >> README.md
            echo "*Auto-updated by GitHub Actions 🤖 (PR Debug로 실제 업데이트됨)*" >> README.md
            
            echo "✅ 실제 제출 현황 반영 README 생성 완료"
            echo "UPDATE_SUCCESS=true" >> $GITHUB_ENV
          fi
          
      - name: Show generated README preview
        run: |
          echo "📄 생성된 README.md 미리보기 (첫 50줄):"
          echo "================================================"
          head -50 README.md
          echo "================================================"
          echo "(총 $(wc -l < README.md) 줄, $(stat -c%s README.md) bytes)"
          
      - name: Actually commit README changes
        if: env.UPDATE_SUCCESS == 'true' && (github.event.inputs.actually_commit != 'false')
        run: |
          echo "💾 실제 README.md 커밋 중..."
          
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action - PR Debug Update"
          
          # 변경된 파일들 추가
          git add README.md
          git add $BACKUP_FILE 2>/dev/null || true
          
          if git diff --staged --quiet; then
            echo "📝 변경 사항이 없습니다."
            echo "ACTUALLY_COMMITTED=false" >> $GITHUB_ENV
          else
            echo "📝 실제 변경사항 커밋:"
            git diff --staged --name-only
            
            COMMIT_TIME=$(date '+%Y-%m-%d %H:%M:%S KST')
            PR_INFO=""
            if [ "${{ github.event_name }}" = "pull_request" ]; then
              PR_INFO=" (PR #${{ github.event.number }}에 의해 트리거됨)"
            fi
            
            git commit -m "🔄 ${SESSION_NUMBER}회차 README 자동 업데이트${PR_INFO}

            📅 기간: ${WEEK_START} ~ ${WEEK_END}
            ⏰ 마감: ${DEADLINE}
            🔧 트리거: ${{ github.event_name }}
            👤 실행자: ${{ github.actor }}
            🕐 업데이트 시간: $COMMIT_TIME

            ✨ 실제 제출 현황을 스캔하여 반영했습니다."
                        
            git push origin main
            
            echo "✅ README.md 실제 커밋 및 푸시 완료!"
            echo "ACTUALLY_COMMITTED=true" >> $GITHUB_ENV
          fi
          
      - name: Send actual notification
        if: env.ACTUALLY_COMMITTED == 'true' && github.event.inputs.notification == 'true'
        env:
          YEOMIN4242_MATTERMOST_URL: ${{ secrets.YEOMIN4242_MATTERMOST_URL }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "📢 실제 README 업데이트 알림 전송..."
          
          if [ -f "scripts/send_notification.py" ]; then
            echo "📨 scripts/send_notification.py 실행 중..."
            
            cd scripts
            python send_notification.py --debug-mode
            cd ..
            
            echo "✅ 알림 전송 완료"
          else
            echo "❌ scripts/send_notification.py 없음 - 기본 알림"
            
            if [ -n "$YEOMIN4242_MATTERMOST_URL" ]; then
              MESSAGE="🔄 **README 실제 업데이트 완료!**

          📅 **${SESSION_NUMBER}회차**: ${WEEK_START} ~ ${WEEK_END}
          ⏰ **마감**: ${DEADLINE}
          🔧 **트리거**: ${{ github.event_name }}
          👤 **실행자**: ${{ github.actor }}
          🕐 **시간**: $(date '+%Y-%m-%d %H:%M:%S KST')

          ✅ 실제 제출 현황이 README에 반영되었습니다!
          🔗 **확인하기**: https://github.com/${{ github.repository }}"

              curl -X POST "$YEOMIN4242_MATTERMOST_URL" \
                -H "Content-Type: application/json" \
                -d '{"text":"'"$MESSAGE"'"}' || echo "알림 전송 실패"
            else
              echo "⚠️ Mattermost webhook URL이 설정되지 않음"
            fi
          fi
          
      - name: Add PR comment with actual results
        if: github.event_name == 'pull_request' && env.UPDATE_SUCCESS == 'true'
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.payload.number;
            const sessionNumber = process.env.SESSION_NUMBER;
            const updateSuccess = process.env.UPDATE_SUCCESS;
            const actuallyCommitted = process.env.ACTUALLY_COMMITTED;
            
            const comment = `## 🔥 PR 디버그: README 실제 업데이트 완료!
            
            **⏰ 실행 시간**: ${new Date().toLocaleString('ko-KR', {timeZone: 'Asia/Seoul'})} KST
            **🎯 회차**: ${sessionNumber}회차 (실제 업데이트)
            **📝 README 생성**: ${updateSuccess === 'true' ? '✅ 성공' : '❌ 실패'}
            **💾 실제 커밋**: ${actuallyCommitted === 'true' ? '✅ 완료' : '❌ 변경사항 없음'}
            
            ### 📋 실제 수행된 작업
            - ✅ main 브랜치에서 실제 파일 스캔
            - ✅ 실제 제출 현황 반영
            - ✅ README.md 파일 실제 업데이트
            - ✅ Git 커밋 및 푸시 완료
            
            ### 🔧 스캔 결과
            - **실제 repository 파일 스캔 완료**
            - **사용자별 문제 폴더 자동 인식**
            - **Main.java 파일 존재 여부 확인**
            - **제출 현황 테이블 자동 생성**
            
            ### 📊 업데이트 정보
            - **회차**: ${sessionNumber}회차
            - **기간**: ${process.env.WEEK_START} ~ ${process.env.WEEK_END}
            - **마감**: ${process.env.DEADLINE}
            
            > 🎉 **완료**: 이제 main 브랜치의 README.md가 실제로 업데이트되었습니다!
            > 
            > 📝 **확인**: [최신 README 보기](https://github.com/${context.repo.owner}/${context.repo.repo})
            
            ---
            *🤖 실제 README 업데이트 완료 - PR #${prNumber}에 의해 트리거됨*`;
            
            github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
            
      - name: Final summary
        if: always()
        run: |
          echo "## 🔥 PR Debug - 실제 README 업데이트 완료!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**⚠️ 주의: 이번에는 실제로 main 브랜치 README가 업데이트되었습니다!**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**실행 시간**: $(date '+%Y-%m-%d %H:%M:%S KST')" >> $GITHUB_STEP_SUMMARY
          echo "**트리거**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "**PR 번호**: #${{ github.event.number }}" >> $GITHUB_STEP_SUMMARY
            echo "**PR 작성자**: ${{ github.event.pull_request.user.login }}" >> $GITHUB_STEP_SUMMARY
            echo "**브랜치**: ${{ github.event.pull_request.head.ref }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 실제 업데이트 결과" >> $GITHUB_STEP_SUMMARY
          echo "- **회차**: ${SESSION_NUMBER}회차" >> $GITHUB_STEP_SUMMARY
          echo "- **기간**: ${WEEK_START} ~ ${WEEK_END}" >> $GITHUB_STEP_SUMMARY
          echo "- **마감**: ${DEADLINE}" >> $GITHUB_STEP_SUMMARY
          echo "- **README 생성**: ${UPDATE_SUCCESS}" >> $GITHUB_STEP_SUMMARY
          echo "- **실제 커밋**: ${ACTUALLY_COMMITTED}" >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎯 수행된 작업" >> $GITHUB_STEP_SUMMARY
          echo "✅ **main 브랜치 체크아웃**: 실제 업데이트를 위해" >> $GITHUB_STEP_SUMMARY
          echo "✅ **실제 파일 스캔**: repository의 모든 사용자 폴더" >> $GITHUB_STEP_SUMMARY
          echo "✅ **제출 현황 반영**: 실제 Main.java 파일 기반" >> $GITHUB_STEP_SUMMARY
          echo "✅ **README 생성**: 실제 데이터로 테이블 구성" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Git 커밋**: main 브랜치에 실제 푸시" >> $GITHUB_STEP_SUMMARY
          
          if [ "$BACKUP_FILE" != "" ]; then
            echo "📁 **백업**: $BACKUP_FILE" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 확인하기" >> $GITHUB_STEP_SUMMARY
          echo "- [📝 업데이트된 README 보기](https://github.com/${{ github.repository }})" >> $GITHUB_STEP_SUMMARY
          echo "- [📈 커밋 히스토리 보기](https://github.com/${{ github.repository }}/commits/main)" >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*🔥 실제 README 업데이트 완료 - PR 트리거로 main 브랜치 업데이트!*" >> $GITHUB_STEP_SUMMARY